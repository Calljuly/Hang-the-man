import React from 'react'

const guesses = (props) => {
    
    return (
        <div>
            <p>{props.word}</p>
            <p>Antal gissningar : 0</p>
        </div>
    )
}
export default guesses
